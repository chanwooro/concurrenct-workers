{
  "name": "@cwr/concurrency-workers",
  "version": "0.0.0",
  "main": "dist",
  "scripts": {
    "build": "run-s clean && run-p build:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "release-it": "^16.2.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
